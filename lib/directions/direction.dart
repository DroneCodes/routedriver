import 'package:flutter/material.dart';
import 'package:routedriver/directions/background.dart';

class Directions extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BackgroundImage(),
    );
  }
}
